# Repo Triage

When solving repository questions:

1. Start with `list_repo_files` to learn the layout.
2. Use `search_repo_text` to locate candidate symbols or constants.
3. Use `read_repo_file` to confirm the exact final answer.
4. Answer with the exact requested value and no extra prose.
