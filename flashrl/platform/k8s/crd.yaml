apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: flashrljobs.platform.flashrl.dev
spec:
  group: platform.flashrl.dev
  scope: Namespaced
  names:
    plural: flashrljobs
    singular: flashrljob
    kind: FlashRLJob
    shortNames:
    - frj
  versions:
  - name: v1alpha1
    served: true
    storage: true
    schema:
      openAPIV3Schema:
        additionalProperties: false
        description: Validated FlashRLJob custom resource.
        properties:
          apiVersion:
            const: platform.flashrl.dev/v1alpha1
            default: platform.flashrl.dev/v1alpha1
            type: string
          kind:
            const: FlashRLJob
            default: FlashRLJob
            type: string
          metadata:
            type: object
            x-kubernetes-preserve-unknown-fields: true
          spec:
            additionalProperties: false
            description: Top-level FlashRLJob spec.
            x-kubernetes-preserve-unknown-fields: true
            type: object
          status:
            additionalProperties: false
            description: Controller and operator managed status fields.
            x-kubernetes-preserve-unknown-fields: true
            type: object
        required:
        - spec
        type: object
    subresources:
      status: {}
